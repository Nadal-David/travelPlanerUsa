// Compatibility aliases consumed by the application.
// Keeping them here makes the individual data files independent and reusable.
const tripDays = window.TravelPlannerData.days;
const { overviewTrip, estaInfo, parkPassInfo, esimInfo, nationalParks, prepTodo, travelTodo } = window.TravelPlannerData;
