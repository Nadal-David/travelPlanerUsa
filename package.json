{
  "name": "travel-planner-usa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ./assets/styles.css --minify",
    "dev": "tailwindcss -i ./src/styles.css -o ./assets/styles.css --watch",
    "test": "node ./tests/smoke.mjs",
    "routes:generate": "node ./scripts/generate-routes.mjs",
    "routes:optimize": "node ./scripts/optimize-routes.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
